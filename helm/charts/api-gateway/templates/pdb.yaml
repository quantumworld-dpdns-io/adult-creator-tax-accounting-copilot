apiVersion: v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "copilot.fullname" . }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "copilot.selectorLabels" . | nindent 6 }}
