# /tax — Tax-form generation helper

Generate a tax form for the given creator using the MCP tool.

```bash
mcp-server-python --tool file_1099 --creator "$1" --year "$2" --form "$3"
```

Usage: `/tax creator_abc 2026 1099-NEC`
